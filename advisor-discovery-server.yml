server:
  port: 9777
eureka:
  instance:
    hostname: localhost
  client:
    register_with_eureka: false
    fetch_registry: false
    service_url:
      default_zone: http://${eureka.instance.hostname}:${server.port}/eureka/
  server:
    wait_time_in_ms_when_sync_empty: 3