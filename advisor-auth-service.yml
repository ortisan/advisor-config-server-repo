server:
  port: 9666
eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:9777/eureka/

ortiz:
  auth:
    google_token_service_info_url: https://www.googleapis.com/oauth2/v1/tokeninfo?access_token={access_token}